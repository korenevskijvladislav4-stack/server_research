# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=research_crm

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Superuser seed (one-time)
SUPERUSER_USERNAME=admin
SUPERUSER_EMAIL=admin@example.com
SUPERUSER_PASSWORD=ChangeMe123!

# Proxy Settings for Slot Parsing
# 
# Option 1: Built-in Proxy Providers (Recommended - automatic country selection)
# Just set PROXY_PROVIDER and credentials, system will automatically select proxy by country code (DE, RU, EN, etc.)
# You just pass country code (DE, RU, EN) and system handles the rest!
# 
# Bright Data / Luminati (https://brightdata.com/) - Enterprise solution
# 
# Формат 1: Новый формат Bright Data (рекомендуется)
# PROXY_PROVIDER=brightdata
# BRIGHTDATA_USER=brd-customer-{your_customer_id}
# BRIGHTDATA_ZONE=datacenter  # или название вашей зоны (datacenter, residential, mobile)
# BRIGHTDATA_PASS=your_password
# BRIGHTDATA_HOST=brd.superproxy.io  # опционально, по умолчанию используется этот хост
# BRIGHTDATA_PORT=33335  # опционально, по умолчанию 33335 (старый порт 22225 устарел)
#
# Формат 2: Старый формат Luminati (если используете старый аккаунт)
# PROXY_PROVIDER=brightdata
# BRIGHTDATA_USER=your_luminati_username
# BRIGHTDATA_PASS=your_password
#
# Пример для нового формата:
# PROXY_PROVIDER=brightdata
# BRIGHTDATA_USER=hl_03a7aea4
# BRIGHTDATA_ZONE=datacenter
# BRIGHTDATA_PASS=8523988b-f4fb-4182-b4f0-f230fe99fd3c
#
# Oxylabs (https://oxylabs.io/) - Enterprise solution
# PROXY_PROVIDER=oxylabs
# OXYLABS_USER=your_username
# OXYLABS_PASS=your_password
#
# Smartproxy (https://smartproxy.com/) - Enterprise solution
# PROXY_PROVIDER=smartproxy
# SMARTPROXY_USER=your_username
# SMARTPROXY_PASS=your_password
#
# Option 2: Manual Proxy Configuration
# Format: host:port or host:port:username:password
# Multiple proxies can be separated by comma: host1:port1,host2:port2
# Example: PROXY_RU=proxy.example.com:8080:user:pass
PROXY_RU=
PROXY_DE=
PROXY_BR=
PROXY_EN=
PROXY_FR=
PROXY_ES=
PROXY_IT=
PROXY_PL=

# IMAP Configuration
# For Mail.ru: imap.mail.ru, port 993, use password for external applications if 2FA is enabled
# For Gmail: imap.gmail.com, port 993, use App Password
IMAP_HOST=imap.mail.ru
IMAP_PORT=993
IMAP_USER=your_email@mail.ru
IMAP_PASSWORD=your_password_or_app_password
IMAP_TLS=true

# CORS (разработка)
CORS_ORIGIN=http://localhost:3000

# CORS для production (опционально). Если задано — в проде используется вместо CORS_ORIGIN.
# Несколько доменов через запятую: https://app.example.com,https://www.example.com
# CORS_ORIGIN_PROD=https://research.supporthelper.website,https://www.research.supporthelper.website
